mutation MakeTurn($confirmed: Boolean, $word: [CellInput!]!, $gameId: Float!, $socketId: String!) {
  makeTurn(confirmed: $confirmed, word: $word, gameId: $gameId, socketId: $socketId) {
    ...GameFragment
  }
}
